/*Write a function slice() to slice a string. It should change the original string such that it is now the sliced string. Take m and n as the start and ending position for slice.*/
#include<stdio.h>

int main(){
    char str[] = "Dipesh";
    int i = 0, count;
    char c = str[i];
    while (c!='\0')
    {
        c = str[i];
        i++;
    }
    count = i - 1;
    printf("String contains %d character.", count);
    if (count%2 == 0)
    {
        int chr = 0;
        while (char ==)
        {
            /* code */
        }
        
    }
    
    return 0;
}